Run from the extracted folder on a clean Ubuntu/Debian VPS:

```bash
sudo apt update && sudo apt install -y curl && sudo bash -c 'bash <(curl -fsSL https://raw.githubusercontent.com/drmmya/lottie/main/lottie-install-vps.sh)'
```
